{
    "primary_key": {
        "title": "Primary Key",
        "description": "The columns to use as primary key in Keboola Storage",
        "type": "array",
        "uniqueItems": true,
        "minItems": 1,
        "propertyOrder": 8000,
        "format": "select",
        "items": {
            "type": "string",
            "enum": []
        }
    }
}